import type { NullableList } from "../NullableList";

/**
 * 
 * @param rootDir 在此目錄下搜尋
 * @param includes 包含的文件
 * @param excludes 排除的文件
 */
export function regexMatchFiles(
	rootDir: string
	,includes?: NullableList<RegExp>
	,excludes?: NullableList<RegExp>
){

}